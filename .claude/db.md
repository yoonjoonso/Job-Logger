# DB

SQLite lives at `data/job-log.db`.

- `npm run db:init` - full init / reseed
- `npm run db:roles` - reseed roles only
- `npm run db:signals` - reseed signals only
- `npm run query` - DB query helper
